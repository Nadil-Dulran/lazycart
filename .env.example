# Prisma / Database
# Replace with your actual credentials and host
DATABASE_URL="postgresql://user:password@localhost:5432/lazycart?schema=public"

# Optional: direct connection URL used by Prisma for certain operations
DIRECT_URL="postgresql://user:password@localhost:5432/lazycart?schema=public"

# Next.js example vars (adjust as needed)
NEXT_PUBLIC_APP_URL="http://localhost:3000"
NODE_ENV="development"